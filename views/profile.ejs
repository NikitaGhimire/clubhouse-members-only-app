<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
</head>
<body>
    <h1>Welcome, <%= user.email %>!</h1>
    <p>Your full name: <%= user.first_name %> <%= user.last_name %></p>
    <p>Membership status: <%= user.membership_status ? 'Member' : 'Non-member' %></p>
    <p>Admin status: <%= user.admin ? 'Admin' : 'Regular User' %></p>
    <a href="/membership">Update Membership Status</a>
    <a href="/logout">Logout</a>
</body>
</html>
